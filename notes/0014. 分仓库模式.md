---
id: c14adee1-6056-4fbc-bf9e-97340b3d7552
description: 本文介绍了 TNotes 采用分仓库模式管理笔记的原因、知识库划分标准以及如何创建和查看知识库。
---

## 1. 本节内容

这篇笔记记录了在设计 TNotes 的时候，为何决定要分仓库来管理笔记，以及仓库划分的策略。

主要记录在设计时的一些思考心路历程。

## 2. 分仓库模式

思考：

- 为什么要分仓库来管理笔记？
- 直接将所有的学习笔记合并到一个仓库中不行吗？

### 2.1. 核心问题

若所有笔记都统一丢到一个仓库中，后续仓库体积和笔记数量必然会变得异常庞大，不便于管理。

```mindmap
# 单仓 vs 多仓

- 在单仓库模式下
  - 全量的 git push、git clone、git pull 操作耗时会显著增加
  - github actions 跑自动构建也会变得极其缓慢，每次 push 完之后需要等很长时间才能在 github pages 中完成部署
  - 随着体积和数量变多，单库的 Desk 打开、`tn:update` / `tn:build` 也会变慢，影响书写体验
  - ……
- 在分仓库模式下
  - 基于分仓库的模式，可以确保任意单一知识库都能独立运行，独立部署。
  - 临时设备上可以只 clone 某一个 `TNotes.xxx`，用 Desk 打开该目录就能写；要发 Pages 再 `pnpm install`。各库彼此独立。
  - 更符合真实 kb 的笔记编写场景：比如现在正在学习算法和数据结构，此时通常只需要关注和算法数据结构直接相关的笔记即可，如果所有笔记混在一个 git 仓库中，就无法很好地去定位相关笔记。
```

### 2.2. 当前的笔记架构

```mindmap
# TNotes.root（根知识库）

- TNotes.algorithms（子知识库）
- TNotes.c（子知识库）
- TNotes.canvas（子知识库）
- ……
- 每一个节点都是一个 git 仓库，所有仓库的笔记目录汇总到根知识库 TNotes.root 中实现快速导航功能。
```

你可以在 github 上的 tnotesjs 组织下查看 [TNotes 知识库列表][1]，所有公开分享的开源 kbs 都丢到了这里边儿。

![tnotesjs 的 repositories 列表](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-10-03-19-09-42.png)

## 3. 知识库的划分标准

关于“知识库的划分标准”，这一点因人而异，没有统一的答案。比如 `TNotes.canvas`、`TNotes.electron`、`TNotes.typescript` 技术栈都可以直接划分到 `TNotes.front-end` 前端这个知识库中，也可以拆分为独立的知识库存在。这里记录一下我的处理策略，在创建知识库的时候可以考虑以下问题，若满足，则定义为一个独立的知识库。

```mindmap [知识库的划分标准]
- 主要看市面上是否有对此“知识”的陈体系资料
- 思考：是否有对应的书籍或系统的视频教程？
  - 示例：`TNotes.typescript` 就问问自己市面上是否有对应的书籍或者视频教程来专门介绍 typescript 呢？
  - 如果答案是肯定的，那么就直接创建一个独立的知识库来管理和 typescript 相关的笔记。
  - 如果答案是否定的，或者很少的资料来专门介绍它，那么说明它很可能是一个比较散的知识点，大概率是属于某个更大的知识体系下的子级，不应该将其定义为一个独立的知识库。
```

## 5. 引用

- [tnotesjs - repositories][1]

[1]: https://github.com/orgs/tnotesjs/repositories
